# ======================
# PROD PROFILE CONFIG
# ======================

# OpenAI (ONLY in PROD)
spring.ai.openai.api-key=${OPENAI_API_KEY}
spring.ai.openai.chat.options.model=gpt-4o-mini
spring.ai.openai.embedding.model=text-embedding-3-small

spring.ai.model.chat=openai

# ❌ NO OLLAMA IN PROD

# Vector DB (pgvector - PROD)
spring.ai.vectorstore.pgvector.enabled=true
spring.ai.vectorstore.pgvector.initialize-schema=false
spring.ai.vectorstore.pgvector.dimensions=1536
spring.ai.vectorstore.pgvector.index-type=hnsw

# RAG tuning
helpdesk.rag.chunk-size=350
helpdesk.rag.chunk-overlap-words=40
helpdesk.rag.include-source-prefix=true
helpdesk.rag.company-docs.similarity-threshold=0.60
helpdesk.rag.company-docs.top-k=5

# Performance
spring.main.lazy-initialization=true

# Logging
logging.level.root=INFO